message: "I am Spring-SQL service in dev environment"

spring:
  application:
    name: springpgsql
  datasource:
    url: jdbc:postgresql://localhost/testdb
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true